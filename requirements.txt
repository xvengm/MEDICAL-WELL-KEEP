# Web
fastapi==0.115.0
jinja2==3.1.4
python-dotenv==1.0.1

# LLM/Gemini (let the adapter pick its google-generativeai)
langchain==0.2.12
langchain-community==0.2.11
langchain-google-genai==1.0.6
httpx==0.27.2
pydantic==2.8.2
requests==2.32.3

# Optional lightweight PDF text only (remove if not needed in the request path)
pypdf==4.3.1

# NOTE: do NOT include:
# - chromadb
# - PyMuPDF, textract, pytesseract, Pillow
# - uvicorn[standard] (you don't run a server on Vercel)
