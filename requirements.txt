# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.1

# Templates
jinja2==3.1.4

# Env
python-dotenv==1.0.1

# LangChain + Gemini
langchain==0.2.12
langchain-community==0.2.11
langchain-google-genai==1.0.6
# DO NOT pin google-generativeai here; let langchain-google-genai choose
httpx==0.27.2          # needed by google-generativeai range

# Vector DB
chromadb==0.5.5

# Docs / OCR (remove if not needed on serverless)
PyMuPDF==1.24.9
python-docx==1.1.2
# textract==1.6.5       # often breaks on Vercel; avoid if possible
pytesseract==0.3.13
Pillow==10.4.0

# Validation / HTTP
pydantic==2.8.2        # avoids known resolver churn with google-genai
requests==2.32.3
